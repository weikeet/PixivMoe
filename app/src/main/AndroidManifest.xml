<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.weicools.pixiv.moe">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="me.weicools.pixivmoe.PixivApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name="me.weicools.pixivmoe.module.login.LoginActivity"></activity>
        <activity android:name="me.weicools.pixivmoe.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="me.weicools.pixivmoe.PixivDetailActivity"
            android:label="@string/title_activity_pixiv_detail"
            android:theme="@style/AppNoActionBarTheme" />
    </application>

</manifest>